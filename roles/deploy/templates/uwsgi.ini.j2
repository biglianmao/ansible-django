[uwsgi]
chdir = {{ virtualenv_dir }}
env = DJANGO_SETTINGS_MODULE={{ virtualenv_dir }}.settings.production
env = DATABASE_URL=postgres://{{ dbuser }}:{{ dbpassword }}@localhost/{{ dbname }}
env = STATIC_ROOT={{ static_dir }}
wsgi-file = {{ virtualenv_dir }}/wsgi.py
workers = 1
